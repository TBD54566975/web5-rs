[package]
name = "dids"
version = "0.1.0"
edition = "2021"

[dependencies]
crypto = { path = "../crypto" }
did-jwk = "0.1.1"
ssi-dids = "0.1.1"
thiserror = "1.0.50"
uniffi = "0.25.1"

[build-dependencies]
uniffi = { version = "0.25.1", features = ["build"] }
uniffi_bindgen = "0.25.1"